# OS / editor junk
.DS_Store
**/.DS_Store
.claude/

# Local parking area for files awaiting manual deletion — never publish
bin/

# Rule: private material (transcripts, CV drafts, reviewer edits, submission
# packages, notes) lives in ../geoffrey-profile-private, NOT in this repo.
